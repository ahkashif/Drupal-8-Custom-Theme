<div id="margin">
  <section{{ attributes.addClass(classes) }}>
  
    {{ title_prefix }}
    {% if lFrom the Firehoseabel %}
      <h2{{ title_attributes.addClass('block-title') }}>{{ label }}</h2>
    {% endif %}
    {{ title_suffix }}

    {% block content %}
      {{ content }}
    {% endblock %}
  </section>
</div>
	